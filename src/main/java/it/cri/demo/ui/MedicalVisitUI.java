package it.cri.demo.ui;

import com.jgoodies.forms.layout.CellConstraints;
import com.jgoodies.forms.layout.FormLayout;
import it.cri.demo.controller.MedicalVisitTableModel;
import it.cri.demo.entity.MedicalVisit;
import it.cri.demo.service.MedicalVisitService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

import javax.swing.*;
import java.awt.*;
import java.util.List;


public class MedicalVisitUI extends JFrame {
    private JPanel mainPanel;
    private JTable table1;
    private JScrollPane scrollPane;

    private final MedicalVisitService medicalVisitService;

    public MedicalVisitUI(MedicalVisitService medicalVisitService) throws HeadlessException {
        super("Medical visit");
        this.medicalVisitService = medicalVisitService;
        $$$setupUI$$$();
        setContentPane(mainPanel);
        setSize(800, 500);
        setVisible(true);
    }

    /**
     * Method generated by IntelliJ IDEA GUI Designer
     * >>> IMPORTANT!! <<<
     * DO NOT edit this method OR call it in your code!
     *
     * @noinspection ALL
     */
    private void $$$setupUI$$$() {
        createUIComponents();
        mainPanel = new JPanel();
        mainPanel.setLayout(new FormLayout("center:d:grow", "center:d:grow"));
        scrollPane = new JScrollPane();
        CellConstraints cc = new CellConstraints();
        mainPanel.add(scrollPane, cc.xy(1, 1, CellConstraints.FILL, CellConstraints.FILL));
        scrollPane.setViewportView(table1);
    }

    /**
     * @noinspection ALL
     */
    public JComponent $$$getRootComponent$$$() {
        return mainPanel;
    }

    private void createUIComponents() {
        // TODO: place custom component creation code here
        List<MedicalVisit> medicalVisitList = medicalVisitService.getAll();
        table1 = new JTable(new MedicalVisitTableModel(medicalVisitList));
    }
}
